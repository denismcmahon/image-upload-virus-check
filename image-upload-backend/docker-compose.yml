version: '3.8'

services:
  node-backend:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    command: bash -c "service clamav-freshclam start && service clamav-daemon start && node server.js"
